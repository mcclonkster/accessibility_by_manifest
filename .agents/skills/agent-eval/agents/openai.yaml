interface:
  display_name: "Agent Eval"
  short_description: "Head-to-head comparison of coding agents (Claude Code, Aider, Codex, etc.) on custom tasks with pass rate, cost, time, and consistency metrics"
  default_prompt: "Use the agent-eval skill."
policy:
  allow_implicit_invocation: true
