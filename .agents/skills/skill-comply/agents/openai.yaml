interface:
  display_name: "Skill Comply"
  short_description: "Visualize whether skills, rules, and agent definitions are actually followed — auto-generates scenarios at 3 prompt strictness levels, runs agents, classifies behavioral sequences, and reports compliance rates with full tool call timelines"
  default_prompt: "Use the skill-comply skill."
policy:
  allow_implicit_invocation: true
