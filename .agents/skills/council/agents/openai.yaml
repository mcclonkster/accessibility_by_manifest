interface:
  display_name: "Council"
  short_description: "Convene a four-voice council for ambiguous decisions, tradeoffs, and go/no-go calls. Use when multiple valid paths exist and you need structured disagreement before choosing."
  default_prompt: "Use the council skill."
policy:
  allow_implicit_invocation: true
