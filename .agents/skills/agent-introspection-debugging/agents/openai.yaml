interface:
  display_name: "Agent Introspection Debugging"
  short_description: "Structured self-debugging workflow for AI agent failures using capture, diagnosis, contained recovery, and introspection reports."
  default_prompt: "Use the agent-introspection-debugging skill."
policy:
  allow_implicit_invocation: true
