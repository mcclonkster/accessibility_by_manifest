interface:
  display_name: "Automation Audit Ops"
  short_description: "Evidence-first automation inventory and overlap audit workflow for ECC. Use when the user wants to know which jobs, hooks, connectors, MCP servers, or wrappers are live, broken, redundant, or missing before fixing anything."
  default_prompt: "Use the automation-audit-ops skill."
policy:
  allow_implicit_invocation: true
