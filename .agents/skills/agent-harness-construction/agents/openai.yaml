interface:
  display_name: "Agent Harness Construction"
  short_description: "Design and optimize AI agent action spaces, tool definitions, and observation formatting for higher completion rates."
  default_prompt: "Use the agent-harness-construction skill."
policy:
  allow_implicit_invocation: true
