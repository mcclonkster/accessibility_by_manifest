# Archive

This directory is historical material.

Use it for reference, archaeology, or examples from earlier workflow attempts.
Do not treat it as the current source of truth for repo direction, naming, or
active implementation boundaries.

Current authority order:

1. `AGENTS.md`
2. `project-file-structure-md/`
3. active code under `accessibility_by_manifest/` and `src/pdf_accessibility/`
4. active tests under `tests/`

What lives here:

- older workflow experiments
- historical notes and package material
- historical test inputs and outputs

What this directory is not:

- the current v0.1.0 implementation contract
- the canonical planning surface
- the authoritative place to infer current package boundaries

If material here conflicts with `AGENTS.md` or the planning files under
`project-file-structure-md/`, prefer the current files and treat `archive/` as
superseded context.
